/.phpunit.cache
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
auth.json
/public/android-hot
/public/ios-hot

# Credential files (keystores, private keys, etc.)
/credentials/

# Release-Artefakte (scripts/release.sh)
/dist/

# Lokale Planungsnotizen
/plans/

# zapstore/nostr signing temp artifacts
offline_event.json
offline_err.log
event.json

# Pest Browser-Test-Screenshots (Fehlschlag-Artefakte)
/tests/Browser/Screenshots

# Playwright-Screenshots/Snapshots (Design-Reviews). .playwright-mcp/ hat eine
# eigene .gitignore; die root-anchored Patterns fangen lose Bilder im Repo-Root
# ab, OHNE die getrackten public/*.png-Assets zu treffen.
/.playwright-mcp/
/*.png
/*.jpg
/*.jpeg

# Lokale SQLite-Dev-DB — wird on-device von NativePHP angelegt/migriert und
# NICHT ins Bundle gegeben (siehe cleanup_exclude_files). Nie committen:
# enthält sonst lokalen Cache/Sessions/Preferences aus Dev-Läufen.
/database/database.sqlite
/database/database.sqlite-shm
/database/database.sqlite-wal
