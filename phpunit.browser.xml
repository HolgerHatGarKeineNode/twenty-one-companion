<?xml version="1.0" encoding="UTF-8"?>
<!--
    Opt-in Browser-Smoke-Suite (echter Chromium via pest-plugin-browser).
    Läuft NICHT in der Standard-phpunit.xml mit, damit der schnelle Unit/Feature-
    Loop und CI ohne installierte Playwright-Browser nicht brechen.

    Start:  scripts/run-browser.sh        (installiert bei Bedarf Chromium)
        oder php vendor/bin/pest -c phpunit.browser.xml

    PORTAL_URL zeigt bewusst auf einen unerreichbaren Port: die Seiten rendern
    so ihre Offline-/Leer-Zustände (deterministisch, ohne externes Portal) — der
    Smoke prüft Render- und JS-Fehlerfreiheit, nicht die Portal-Daten.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Browser">
            <directory>tests/Browser</directory>
        </testsuite>
    </testsuites>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <env name="PORTAL_URL" value="http://127.0.0.1:9"/>
    </php>
</phpunit>
