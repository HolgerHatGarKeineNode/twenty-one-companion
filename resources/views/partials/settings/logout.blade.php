{{-- The ONE sign-out: Portal token AND local Nostr session. Replaces the package's
     `session` partial, which ends the Nostr session only — see the component's head. --}}
<livewire:settings.logout/>
